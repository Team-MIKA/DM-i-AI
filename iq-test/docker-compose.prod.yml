
version: "3"
services:
  emily-iq-test-d3mauh-prod:
    image: emily-iq-test-d3mauh-prod
    container_name: emily-iq-test-d3mauh-prod
    command: python3 ${MAIN} -e ".prod.env"
    restart: always
    ports:
      - ${HOST_PORT}:${CONTAINER_PORT}
    networks:
      - emily-iq-test-d3mauh-prod-network
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - .:/workspace:cached
      - ./:/workspace/data

    runtime: ${RUNTIME}
    environment:
      - NVIDIA_VISIBLE_DEVICES=${NVIDIA_VISIBLE_DEVICES}
      - NVIDIA_DRIVER_CAPABILITIES=${NVIDIA_DRIVER_CAPABILITIES}
    ipc: ${IPC}
networks:
  emily-iq-test-d3mauh-prod-network:
    name: emily-iq-test-d3mauh-prod-network
    driver: bridge
  
